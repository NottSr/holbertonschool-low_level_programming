#include "main.h"
/**
 * mul - Entry point
 * @a: int checked for first integer
 * @b: int checked for second integer
 * Return: Always res (Success)
 */
int mul(int a, int b)
{
	int res;

	res = a * b;
	return (res);
}
